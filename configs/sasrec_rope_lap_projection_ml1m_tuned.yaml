defaults:
  - dataset: MovieLens-1M
  - model: sasrec_rope_lap_projection
  - metrics: base
  - _self_

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${model.name}__${dataset.name}__${now:%H-%M-%S}
  sweep:
    dir: outputs/sweeps/${now:%Y-%m-%d}/${model.name}__${dataset.name}__${now:%H-%M-%S}

seed: 42
max_top_n: 100

model:
  _target_: src.models.sasrec_rope_lap_projection.SASRecRoPELapProjection
  name: sasrec_rope_lap_projection_ml1m_tuned
  seed: 42
  device: cuda
  lr: 0.006770186482827876
  n_epochs: 1000
  hidden_size: 128
  num_blocks: 2
  num_heads: 2
  dropout: 0.2
  seq_len: 200
  batch_size: 128
  filter_seen: true
  max_patience: 10
  k: 128
  lap_projection_mode: v
  lap_eigvec_path: MovieLens-1M_eigvecs.npy
  lap_alpha: 0.01
  learnable_lap_gate: false
  lap_gate_init: -3.0
  rope_base: 10000.0
