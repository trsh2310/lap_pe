_target_: src.models.SASRecModel
name: "sasrec"
seed: 42
device: "cuda"

lr: 0.007155250682439438
n_epochs: 1000

hidden_size: 64
num_blocks: 2
num_heads: 1
dropout: 0.5
seq_len: 200
batch_size: 256
filter_seen: True
use_absolute_pe: True
max_patience: 10
